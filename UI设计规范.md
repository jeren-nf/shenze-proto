# 申泽跨境贸易服务平台 · UI 设计规范 V1

> 适用端：微信小程序（卖家端 `s2*` / 买家端 `b*`）· 移动端 390×844
> 状态：浅色（Light-only）· 已落地于 7 个细化页面
> 配套：`tokens.css`（设计变量）+ 各页 HTML

---

## 1. 设计定位与背景

- **关系**：本平台是「专业市场子平台」（申泽 / 鞋贸汇），嵌套于母平台 **华商GO**（省电子口岸出品、省属国企背景的"出海综合服务港"）生态，功能上已内嵌母平台链接。
- **目标**：视觉与母平台**同源可信**（一眼像"华商GO 生态里的子平台"），同时用一枚**橙色强调色**保留子平台"专业市场"的个性锚点。
- **调性关键词**：稳健可信 · 现代服务港 · 明快但不轻浮。

---

## 2. 色彩系统

| Token | HEX | 用途 |
|---|---|---|
| `--color-primary` | `#1677FF` | 品牌主色（科技蓝）：链接、图标、主按钮、tab active |
| `--color-primary-hover` | `#4096FF` | 主色 hover |
| `--color-primary-active` | `#0958D9` | 主色按下 |
| `--color-primary-bg` | `#E6F4FF` | 浅蓝底：选中背景、tab active 丸、图标容器 |
| `--color-primary-border` | `#91CAFF` | 聚焦环 / 描边 |
| `--color-accent` | `#F5A623` | **子平台个性橙**：市场/交易/商机主行动 |
| `--color-accent-hover` | `#FFB340` | 橙色 hover |
| `--color-accent-active` | `#D98E0A` | 橙色按下 |
| `--color-accent-bg` | `#FFF7E6` | 橙色浅底：金刚区橙图标容器、橙标签 |
| `--color-accent-border` | `#FFD591` | 橙描边 |
| `--bg-page` | `#EDF7FF` | **页面底（浅蓝空气感，对齐母平台）** |
| `--bg-container` | `#FFFFFF` | 卡片 / 面板 / 弹窗 |
| `--bg-hover` | `#F5F5F5` | 列表 hover |
| `--ink-primary` | `#1A1A1A` | 标题 / 重要正文 |
| `--ink-secondary` | `#595959` | 正文 / 说明 |
| `--ink-tertiary` | `#8C8C8C` | 辅助 / 占位 / 禁用 |
| `--border-weak` | `#F0F0F0` | 卡片内分割 |
| `--border-strong` | `#D9D9D9` | 输入框 / 外框 |
| `--color-error` | `#FF4D4F` | 价格红 / 错误 / 角标 |
| 功能色 | `#52C41A` / `#FAAD14` / `#FF4D4F` | 成功 / 警告 / 错误 |

> 红 `#FF4D4F` 用于**交易价格信号**（供应卡价格），属语义高亮，非按钮。

---

## 3. 渐变系统（母平台视觉血缘）

- `--gradient-brand-start: #00C9A7`（青绿）→ `--gradient-brand-end: #1677FF`（蓝），角度 `135deg`。
- 用法：卖家/买家「我的」页顶部大色块、首页营销 banner、顶部 header。
- 这是"一眼像华商GO"的最大动作，禁止回退为纯色蓝。

---

## 4. 圆角 / 间距

- 圆角：`--r-lg: 16px`（卡片，对齐母平台大白卡）· `--r-md: 8px`（按钮/输入框）· `--r-sm: 4px`（标签）· `--r-pill: 999px`（丸/标签）。
- 间距：8pt 栅格 `--sp-1:4` `--sp-2:8` `--sp-3:12` `--sp-4:16` `--sp-5:24` `--sp-6:32`。

---

## 5. 字体排印

- 字体：`--font-cn` PingFang SC / 微软雅黑 / Noto Sans SC。
- 字号：`--fs-h1:24` `--fs-h2:17` `--fs-h3:15` `--fs-body:14` `--fs-caption:12` `--fs-micro:11`。
- 行高正文 1.6；标题 1.25–1.3。

---

## 6. 阴影 / 层级（克制）

- `--shadow-hover: 0 2px 8px rgba(0,0,0,.06)`
- `--shadow-overlay: 0 6px 24px rgba(0,0,0,.12)`
- 卡片优先以"白卡浮于浅蓝底 + 细边框"建立层级，避免重阴影；仅悬浮按钮（FAB）与遮罩保留明显投影。

---

## 7. 组件规范

| 组件 | 规范 |
|---|---|
| 顶部渐变头 | 青绿→蓝渐变（135deg），与页面底 `#EDF7FF` 用同色弧线衔接；橙色"未认证"状态点 + 文字 |
| 首页 banner | 同品牌渐变，双行（副标题弱化 14px / 主标题 21px 800） |
| 底部 Tab | active = 图标坐浅蓝丸背景 `#E6F4FF` + 文字/图标转 `#1677FF`（不只变色，加丸背景） |
| 卡片 | 白底 `#FFF` + `1px` 细边框 + `16px` 圆角，浮于浅蓝页面底 |
| 图标容器 | 彩色圆角块（蓝/橙/绿/红 浅底 + 同色描边图标） |
| 金刚区 | 4 列网格，图标容器 46px / 圆角 14px |
| 状态标签 | 双轨：流程态橙/蓝，结果态绿/红，历史灰（见 `tokens.css` `--tag-*`） |
| 认证引导 | 浅蓝 `#E8F3FF` 卡片 + 圆角 8pt + 内距 12pt，仅未认证显示，首次加载 2–3 次抖动+光晕 |

---

## 8. 按钮使用规范（关键）

**两套主按钮，语义严格分工：**

| 类型 | 类 | 颜色 | 用途 |
|---|---|---|---|
| 主按钮（信任） | `.btn .btn-primary` | 蓝 `#1677FF` | 认证、提交、通用功能、筛选、返回——与母平台同源的"信任色" |
| **强调按钮（市场）** | `.btn .btn-accent` | **橙 `#F5A623`** | **市场/交易/商机类主行动** |

**橙色 CTA 落地清单（每条上下文仅 1 个橙色主行动，避免滥用）：**

- `s2_home` 悬浮按钮「发供应」、 `b2_home` 悬浮按钮「发求购」→ 橙色（最核心的市场发布动作）
- `b_supply` / `b2_home` 供应卡「立即询价」→ 橙色（买家发起交易）
- `s2_business` 求购卡「立即对接」→ 橙色（卖家连接商机）
- 认证页「提交认证」**保持蓝色**（属信任/准入动作，不占用橙色语义）

> 规则：橙色 = "促成一笔买卖"的最高优先动作；蓝色 = 平台功能与信任动作。价格红维持独立语义。

---

## 9. 无障碍（WCAG）

- **对比度**：正文 ≥ 4.5:1；图形/状态元素 ≥ 3:1。
- **非颜色线索（1.4.1）**：状态不只靠颜色区分。例：未认证"待解锁"用 `1px 虚线环 #C9CED6` + 加深灰图标（`#9AA1AC`），而非纯灰不可见。
- **可点性**：灰态入口仍保留点击区与 active 反馈，避免被读成"禁用"。
- **语义化**：图标 + 文字并用；行动按钮带明确文案，不依赖形状猜测。

---

## 10. 已落地页面

| 页面 | 内容 |
|---|---|
| `s2_mine.html` | 顶部渐变头、浅蓝底、16px 卡片、tab 丸、未认证灰态虚线环、认证引导 |
| `b2_mine.html` | 同上（买家端无认证模块） |
| `s2_home.html` | 渐变 banner、橙色「发供应」FAB、tab 丸 |
| `b2_home.html` | 渐变 banner、橙色「发求购」FAB + 供应卡「立即询价」、tab 丸 |
| `s2_business.html` | 求购卡「立即对接」橙色 CTA、tab 丸 |
| `b_supply.html` | 供应卡「立即询价」橙色 CTA、tab 丸 |
| `s2_verify.html` | 商家认证表单（蓝色提交，保持信任色） |

---

## 11. 二期 / 待办

- 图标 3D 化（向母平台彩色 3D 图标对齐，作为子平台个性资产）
- 橙色 CTA 扩展至"热门求购/供应"横滑卡（如需增强）
- 品牌 logo 正式稿替换 `logo.png` 占位
